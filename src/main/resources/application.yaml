spring:
  application:
    name: msif

  security:
    jwt:
      secret-key: qrVEBGGghpkc9Ra5L1ELHxm6gisrpgMw
      expiration: 1800000
      refresh-token:
        expiration: 604800000

  redis:
    host: localhost
    port: 6379
    timeout: 2000ms

  datasource:
    url: jdbc:postgresql://db.puemwmgbryqpcmaopens.supabase.co:5432/postgres
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

# Hikari defaults
spring.datasource.hikari.maximum-pool-size: 3
