spring:
  application:
    name: msif

  security:
    jwt:
      secret-key: qrVEBGGghpkc9Ra5L1ELHxm6gisrpgMw
      expiration: 1800000
      refresh-token:
        expiration: 604800000

  redis:
    host: localhost
    port: 6379
    timeout: 2000ms

  datasource:
    url: jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres
    username: postgres.puemwmgbryqpcmaopens
    password: root123
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ianbazancontacto@gmail.com
    password: lasv gyak ouet zoex
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
